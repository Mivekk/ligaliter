subscription UpdatePlayerStats($uuid: String!) {
  updatePlayerStats(uuid: $uuid) {
    id
    activePlayer {
      id
      username
    }
    players {
      id
      username
      points
    }
  }
}
