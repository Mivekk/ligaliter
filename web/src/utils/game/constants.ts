export const boardLength = 19;
export const boardSize = boardLength * boardLength;
export const playerTilesAmount = 7;
export const roundTime = 5;
export const boardCenterId =
  boardLength * Math.floor(boardLength / 2) + Math.floor(boardLength / 2);
